[project]
name = "agentic-support"
version = "0.1.0"
description = "A grounded support-triage agent built on Google ADK"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-adk>=2.8.0",
    "python-dotenv>=1.0",
    "pyyaml>=6.0",
]
